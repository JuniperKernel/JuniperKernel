CPPFLAGS := $(ALL_CPPFLAGS) -I../xeus/include -I../../inst/include/
CXXFLAGS := $(ALL_CPPFLAGS) -I../xeus/include -I../../inst/include/
LDLIBS := $(ALL_LIBS) @RINSIDE@ -L.. -lxeus
sources :=    $(wildcard *.cpp)
programs :=     $(sources:.cpp=)


all: $(programs)
	@test -x /usr/bin/strip && strip $^


clean:
	rm -vf $(programs)
	rm -vrf *.dSYM
